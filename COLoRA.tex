%%
%% This is file `sample-sigconf.tex',
%% generated with the docstrip utility.
%%
%% The original source files were:
%%
%% samples.dtx  (with options: `all,proceedings,bibtex,sigconf')
%% 
%% IMPORTANT NOTICE:
%% 
%% For the copyright see the source file.
%% 
%% Any modified versions of this file must be renamed
%% with new filenames distinct from sample-sigconf.tex.
%% 
%% For distribution of the original source see the terms
%% for copying and modification in the file samples.dtx.
%% 
%% This generated file may be distributed as long as the
%% original source files, as listed above, are part of the
%% same distribution. (The sources need not necessarily be
%% in the same archive or directory.)
%%
%%
%% Commands for TeXCount
%TC:macro \cite [option:text,text]
%TC:macro \citep [option:text,text]
%TC:macro \citet [option:text,text]
%TC:envir table 0 1
%TC:envir table* 0 1
%TC:envir tabular [ignore] word
%TC:envir displaymath 0 word
%TC:envir math 0 word
%TC:envir comment 0 0
%%
%% The first command in your LaTeX source must be the \documentclass
%% command.
%%
%% For submission and review of your manuscript please change the
%% command to \documentclass[manuscript, screen, review]{acmart}.
%%
%% When submitting camera ready or to TAPS, please change the command
%% to \documentclass[sigconf]{acmart} or whichever template is required
%% for your publication.
%%
%%
\documentclass[sigplan,anonymous,review,nonacm]{acmart}
\usepackage{subcaption}
%%
%% \BibTeX command to typeset BibTeX logo in the docs
\AtBeginDocument{%
  \providecommand\BibTeX{{%
    Bib\TeX}}}

%% Rights management information.  This information is sent to you
%% when you complete the rights form.  These commands have SAMPLE
%% values in them; it is your responsibility as an author to replace
%% the commands and values with those provided to you when you
%% complete the rights form.
\setcopyright{acmlicensed}
\copyrightyear{2018}
\acmYear{2018}
\acmDOI{XXXXXXX.XXXXXXX}
%% These commands are for a PROCEEDINGS abstract or paper.
\acmConference[Conference acronym 'XX]{Make sure to enter the correct
  conference title from your rights confirmation email}{June 03--05,
  2018}{Woodstock, NY}
%%
%%  Uncomment \acmBooktitle if the title of the proceedings is different
%%  from ``Proceedings of ...''!
%%
%%\acmBooktitle{Woodstock '18: ACM Symposium on Neural Gaze Detection,
%%  June 03--05, 2018, Woodstock, NY}
\acmISBN{978-1-4503-XXXX-X/2018/06}


%%
%% Submission ID.
%% Use this when submitting an article to a sponsored event. You'll
%% receive a unique submission ID from the organizers
%% of the event, and this ID should be used as the parameter to this command.
%%\acmSubmissionID{123-A56-BU3}

%%
%% For managing citations, it is recommended to use bibliography
%% files in BibTeX format.
%%
%% You can then either use BibTeX with the ACM-Reference-Format style,
%% or BibLaTeX with the acmnumeric or acmauthoryear sytles, that include
%% support for advanced citation of software artefact from the
%% biblatex-software package, also separately available on CTAN.
%%
%% Look at the sample-*-biblatex.tex files for templates showcasing
%% the biblatex styles.
%%

%%
%% The majority of ACM publications use numbered citations and
%% references.  The command \citestyle{authoryear} switches to the
%% "author year" style.
%%
%% If you are preparing content for an event
%% sponsored by ACM SIGGRAPH, you must use the "author year" style of
%% citations and references.
%% Uncommenting
%% the next command will enable that style.
%%\citestyle{acmauthoryear}


%%
%% end of the preamble, start of the body of the document source.
\begin{document}

%%
%% The "title" command has an optional parameter,
%% allowing the author to define a "short title" to be used in page headers.
\title{COLoRA: Non-Blocking Miss Handling for Multi-LoRA MoE Serving}

%%
%% The "author" command and its associated commands are used to define
%% the authors and their affiliations.
%% Of note is the shared affiliation of the first two authors, and the
%% "authornote" and "authornotemark" commands
%% used to denote shared contribution to the research.
% \author{Ben Trovato}
% \authornote{Both authors contributed equally to this research.}
% \email{trovato@corporation.com}
% \orcid{1234-5678-9012}
% \author{G.K.M. Tobin}
% \authornotemark[1]
% \email{webmaster@marysville-ohio.com}
% \affiliation{%
%   \institution{Institute for Clarity in Documentation}
%   \city{Dublin}
%   \state{Ohio}
%   \country{USA}
% }
\author{Anonymous Submission}

%%
%% By default, the full list of authors will be used in the page
%% headers. Often, this list is too long, and will overlap
%% other information printed in the page headers. This command allows
%% the author to define a more concise list
%% of authors' names for this purpose.
% \renewcommand{\shortauthors}{Trovato et al.}

%%
%% The abstract is a short summary of the work to be presented in the
%% article.
\begin{abstract}
This draft focuses on the problem formulation and design of COLoRA. The implementation and evaluation are still in progress.
\end{abstract}

%%
%% The code below is generated by the tool at http://dl.acm.org/ccs.cfm.
%% Please copy and paste the code instead of the example below.
%%
\begin{CCSXML}
<ccs2012>
   <concept>
       <concept_id>10010520.10010521.10010542.10010546</concept_id>
       <concept_desc>Computer systems organization~Heterogeneous (hybrid) systems</concept_desc>
       <concept_significance>500</concept_significance>
       </concept>
   <concept>
       <concept_id>10011007.10010940.10010941.10010949.10010950</concept_id>
       <concept_desc>Software and its engineering~Memory management</concept_desc>
       <concept_significance>500</concept_significance>
       </concept>
   <concept>
       <concept_id>10011007.10010940.10010941.10010949.10010957.10010688</concept_id>
       <concept_desc>Software and its engineering~Scheduling</concept_desc>
       <concept_significance>500</concept_significance>
       </concept>
 </ccs2012>
\end{CCSXML}

\ccsdesc[500]{Computer systems organization~Heterogeneous (hybrid) systems}
\ccsdesc[500]{Software and its engineering~Memory management}
\ccsdesc[500]{Software and its engineering~Scheduling}

%%
%% Keywords. The author(s) should pick words that accurately describe
%% the work being presented. Separate the keywords with commas.
\keywords{MoE inference, LoRA serving, heterogeneous execution}
%% A "teaser" image appears between the author and affiliation
%% information and the body of the document, and typically spans the
%% page.
% \begin{teaserfigure}
%   \includegraphics[width=\textwidth]{sampleteaser}
%   \caption{Seattle Mariners at Spring Training, 2010.}
%   \Description{Enjoying the baseball game from the third-base
%   seats. Ichiro Suzuki preparing to bat.}
%   \label{fig:teaser}
% \end{teaserfigure}

% \received{20 February 2007}
% \received[revised]{12 March 2009}
% \received[accepted]{5 June 2009}

%%
%% This command processes the author and affiliation and title
%% information and builds the first part of the formatted document.
\maketitle

\section{Introduction}
\label{sec:introduction}

Large-scale LLM serving is increasingly shaped by the convergence of two trends that are independently effective but jointly challenging: sparse Mixture-of-Experts (MoE) base models~\cite{todo:moe-llm} and Low-Rank Adaptation (LoRA)-based multi-tenant serving~\cite{todo:lora-original,todo:lora-serving}. In production systems, these two trends naturally meet in \emph{multi-LoRA MoE serving}. While LoRA is widely used as a lightweight adaptation mechanism, existing multi-LoRA serving systems are typically not organized around expert-layer adaptation in sparse MoE models~\cite{todo:lora-original,todo:lora-serving}. Effectively customizing MoE models requires applying LoRA directly to the expert layers (MoE LoRA), as expert FFN blocks account for a large fraction of model capacity and are a natural target for task-specific adaptation~\cite{todo:moe-knowledge-capacity}. This setting is attractive because MoE reduces base-model compute, while MoE LoRA enables efficient per-tenant customization without replicating the full model. However, it also creates a new systems regime in which highly dynamic sparse expert routing~\cite{todo:moe-routing-dynamics} and long-tailed multi-adapter access~\cite{todo:slora} interact within the same execution pipeline.

This interaction is particularly problematic during the decode phase of prefill--decode deployments~\cite{todo:pd-serving}, where execution is latency-sensitive, operates at small effective batch sizes, and repeatedly performs routing and LoRA-related parameter access once per generated token. In pure MoE serving, the natural unit of management is the expert. In multi-LoRA serving, the unit is the adapter, and prior systems such as S-LoRA~\cite{todo:slora} and VaLoRA~\cite{mi2025empower} therefore adopt adapter-centric designs. In MoE + multi-LoRA decode, however, a request only needs the LoRA residual associated with the \emph{selected experts}, making the true unit of access and reuse a joint expert--LoRA object rather than an expert-only or adapter-only entity. This joint-object regime is further amplified by object size and memory pressure. Because expert layers are implemented as wide feed-forward blocks (FFNs)~\cite{todo:moe-ffn-architecture}, expert-layer LoRA modules are substantially larger than conventional attention-layer LoRAs. Coupled with the already large parameter footprint of the MoE base model~\cite{todo:large-moe-models}, this leaves only a limited GPU memory budget for expert--LoRA residency, enlarging and fragmenting the effective object space and making GPU residency harder to sustain under fixed memory budgets. More importantly, it exposes a fundamental systems mismatch: expert-centric abstractions fit pure MoE serving, and adapter-centric abstractions fit multi-LoRA serving, but neither matches expert--LoRA joint-object access in decode.

Our trace-driven case study, built on Azure LLM serving traces~\cite{zhang2021faster} with synthetic LoRA assignment, shows that this is not a marginal effect but a regime shift. Under joint-key modeling, the top 1000 objects account for only 40.7\% and 32.6\% of accesses under two adapter-mapping scenarios---independent and correlated assignment---compared to 89.1\% under expert-only modeling. Temporal locality degrades accordingly: the finite P99 reuse distance increases from 1,393 events to 2,041 and 4,521 events, while cold first touches rise from 1.0 to 9.7 and 13.6 per 10k events. These results show that multi-LoRA MoE serving does not merely enlarge the object space; it also fragments the effective working set and weakens temporal locality, making the access pattern substantially less cache-friendly.

These structural changes translate directly into system-level degradation under realistic cache budgets. With a shared 2048-object LRU cache, the miss rate increases from 0.00159 (expert-only) to 0.01095 and 0.01476 under joint-key modeling, a 6.9$\times$--9.3$\times$ increase. While the absolute miss rate remains low, these misses are highly skewed and concentrate on tail requests, where even a small number of misses directly dominate P99 latency. In this regime, performance is governed not by average miss rate, but by the worst-case accumulation of cold-object accesses. Synthetic control sweeps further show that the tail penalty emerges early and persists: at a fixed cache slice, the P99 latency proxy rises sharply as the number of LoRAs increases and quickly plateaus at a high level. At the request level, tail latency is dominated not by average degradation but by bursts of cold-object accesses: while an average request touches tens of cold objects, tail requests may touch hundreds to over a thousand. These findings suggest that cache misses in this regime are not rare anomalies but \emph{regime-driven tail events} concentrated on tail requests.

Once misses become a persistent feature of this regime, the conventional load-then-run paradigm becomes suboptimal. Existing GPU-centric systems treat a cache miss as a weight-migration problem, resolving it by transferring parameters from host to device before execution~\cite{todo:gpu-offload}. This approach works well for hot objects with predictable reuse, but breaks down for long-tail decode misses, where batches are small, reuse is uncertain, and host--device transfer latency is directly exposed on the critical path. As a result, the core systems bottleneck shifts from miss avoidance alone to \emph{miss handling}: the key question is no longer only how to keep more objects resident, but how unavoidable cold misses should be served.

This paper presents \textbf{COLoRA}, a decode-time serving runtime for multi-LoRA MoE models built around a \emph{hot/cold dual-path design} for expert--LoRA computation. Under expert--LoRA joint-object caching, working-set expansion and fragmentation make cold misses difficult to eliminate within practical GPU cache budgets. Rather than treating every miss as an immediate blocking \emph{promotion-first} recovery event, COLoRA adopts an \emph{execution-first} default: cache-hit objects continue on the GPU hot path, while cold misses are served through a CPU cold path to complete the current LoRA computation, and GPU promotion is deferred to future reuse. To make this design practical, COLoRA implements an optimized CPU cold path with AVX kernels and activation coalescing, enabling cold-miss handling to overlap with the GPU hot/base path in mixed hit/miss batches. COLoRA further exploits short-range routing locality across adjacent decode tokens to prefetch a small set of likely expert weights into a CPU-side staging path ahead of use, exposing additional overlap opportunities while preserving exact execution semantics.

The key contributions of this work are:
\begin{itemize}
\item \textbf{Problem characterization.} We identify the expert--LoRA joint-object regime in multi-LoRA MoE decode serving and show that working-set expansion and fragmentation make cold misses difficult to eliminate within realistic GPU cache budgets, turning miss handling---rather than cache capacity alone---into a first-order systems problem.

\item \textbf{Miss-handling abstraction.} We introduce a hot/cold dual-path serving design for expert--LoRA computation in decode. Its key abstraction change is to replace conventional \emph{promotion-first} load-then-run recovery with \emph{execution-first} recovery: hot objects remain on the GPU hot path, while cold misses are completed on a CPU cold path, and promotion is retained only for future reuse.

\item \textbf{System realization.} We implement an efficient CPU cold path with AVX kernels and activation coalescing, making execution-first handling practical under decode-time latency constraints and enabling overlap with the GPU hot/base path in mixed hit/miss batches.

\item \textbf{Overlap-oriented prefetch optimization.} We further exploit short-range routing locality across adjacent decode tokens to prefetch a small set of likely expert weights for the CPU cold path ahead of use. This conservative mechanism preserves exact execution semantics while exposing additional overlap opportunities beyond the main hot/cold miss-handling design.
\end{itemize}

\begin{figure*}[!t]
  \centering
  \begin{minipage}[b]{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig1_popularity_rank.pdf}
    \subcaption{}
  \end{minipage}
  \hfill
  \begin{minipage}[b]{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig2_reuse_distance.pdf}
    \subcaption{}
  \end{minipage}
  \\[8pt]
  \begin{minipage}[b]{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig3_cache_curve.pdf}
    \subcaption{}
  \end{minipage}
  \hfill
  \begin{minipage}[b]{0.49\textwidth}
    \centering
    \includegraphics[width=\linewidth]{figures/fig5_tail_breakdown.pdf}
    \subcaption{}
  \end{minipage}
  \caption{
  Trace-driven case study of expert--LoRA joint-object modeling.
  (a) Popularity becomes significantly less concentrated: under adapter-only modeling (\texttt{C0}), the top-1000 objects account for 89.1\% of accesses, but only 40.7\% and 32.6\% under joint modeling (\texttt{C1}/\texttt{C2}).
  (b) Temporal locality weakens: reuse distance increases substantially (P99: 1{,}393 $\rightarrow$ 2{,}041 $\rightarrow$ 4{,}521), and cold-start frequency rises sharply.
  (c) Under the same cache budget, miss rates increase significantly with joint-object modeling, showing that degraded locality cannot be compensated by fixed-capacity caching.
  (d) Tail requests access up to an order of magnitude more cold objects than average requests, concentrating misses in the tail.
  Together, these results show that joint-object modeling first fragments decode locality and then translates that locality loss into higher miss rates and more severe tail behavior.
  }
  \label{fig:case-study}
\end{figure*}

\section{Background, Motivation, and Challenges}
\label{sec:background}

\subsection{Background: Expert--LoRA Computation in Multi-LoRA MoE Decode}
\label{subsec:background}

We study decode-time serving where a shared sparse MoE base model is combined with multiple LoRA specializations. In this setting, each generated token still triggers dynamic expert routing in the base model, while different requests may invoke different LoRA adapters. As a result, the serving system must repeatedly determine not only which experts are activated, but also which adapter-specific LoRA states are needed on the latency-critical decode path.

This interaction changes the natural unit at which the system should reason about locality and misses. In dense multi-LoRA serving, accesses often concentrate on a single adapter's weights, making adapter-level reasoning sufficient in many cases. In MoE decode, however, each token activates only a small routed subset of experts, and LoRA computation is needed only for the routed experts of the current request. The relevant access unit is therefore not an expert or an adapter in isolation, but their joint occurrence in the decode path, which we refer to as an \emph{expert--LoRA joint object}.

This distinction matters particularly in decode. Decode operates at small effective batch sizes, repeats routing and parameter-access decisions once per token, and exposes miss-handling overheads directly on the latency-critical path. Consequently, when expert--LoRA joint objects do not fit cleanly within practical GPU cache budgets, the key systems question is not only how to manage residency, but also how to recover cold misses without inflating per-token latency.


\subsection{Motivation: Joint-Object Modeling Fragments Decode Locality}
\label{subsec:motivation}

\subsubsection{Expert--LoRA Joint Objects as the Relevant Access Unit}
\label{subsec:joint-object}

A central question is what abstraction the system should use to reason about locality, residency, and misses.

In sparse MoE decode, each token activates only a small subset of routed experts. Once multiple LoRA adapters coexist, accesses no longer concentrate on entire adapters or on experts alone, but on LoRA states attached to specific routed experts. This makes expert--LoRA joint objects the relevant access unit for decode-time serving.

Reasoning at coarser granularity obscures the true access pattern. Adapter-level management is too coarse because it retains many expert slices together with the few that are actually needed for the current token. Expert-level management is incomplete in multi-tenant serving because it ignores adapter identity: while the expert set is fixed, LoRA adapters vary across requests, so collapsing accesses by expert alone systematically overestimates reuse across tenants. To understand the consequences of this change in access granularity, we next compare decode locality and system behavior under adapter-only and expert--LoRA joint-object conditions.


\subsubsection{Joint Objects Fragment Decode Locality}
\label{subsec:case-study}

To examine whether this finer-grained access view matters in practice, we conduct a trace-driven case study under three workload conditions.
\texttt{C0} uses adapter-only keys.
\texttt{C1} uses expert--LoRA joint keys under independent assignment, where adapter identity is sampled independently of expert routing.
\texttt{C2} uses the same expert--LoRA joint keys under correlated assignment, where certain adapters are more likely to co-occur with specific experts.
This setup separates the effect of finer-grained keying itself from the extent to which structured adapter--expert affinity can partially preserve locality.
We use Azure trace~\cite{zhang2021faster} to model LLM request arrivals and Poisson arrivals to model LoRA invocations, and replay the resulting decode access streams under these conditions.

Figure~\ref{fig:case-study} reveals a consistent two-stage effect. Moving from adapter-only to expert--LoRA joint-object modeling first fragments decode locality by flattening popularity and lengthening reuse distance [Fig.~\ref{fig:case-study}(a)--(b)]; this locality loss then manifests at the system level as higher miss rates and more severe tail behavior [Fig.~\ref{fig:case-study}(c)--(d)].

\paragraph{Locality becomes weaker and less concentrated.}
As shown in Fig.~\ref{fig:case-study}(a), joint-object modeling significantly flattens the popularity distribution. Under adapter-only modeling (\texttt{C0}), the top-1000 objects account for 89.1\% of accesses. Under joint modeling, this drops to 40.7\% in \texttt{C1} and 32.6\% in \texttt{C2}. The effective hot set therefore becomes much less dominant, making it harder for any bounded GPU cache to capture a large fraction of accesses.
Fig.~\ref{fig:case-study}(b) shows that temporal locality weakens as well: the P99 reuse distance grows from 1{,}393 events in \texttt{C0} to 2{,}041 and 4{,}521 events in \texttt{C1} and \texttt{C2}, respectively, while cold-start frequency also rises sharply. These longer reuse gaps are especially harmful in decode, where delayed reuse is less likely to be hidden or amortized.

\paragraph{Locality loss translates into system-level inefficiency.}
The locality degradation above directly increases miss pressure. As shown in Fig.~\ref{fig:case-study}(c), miss rates under joint-object modeling fall much more slowly with increasing cache size, indicating that degraded locality cannot be compensated by simply enlarging a fixed-capacity cache. More importantly, Fig.~\ref{fig:case-study}(d) shows that tail requests access up to an order of magnitude more cold objects than average requests. The degradation is therefore not limited to average miss rate: it is concentrated in the tail, where it directly amplifies request latency.

Taken together, these results show that the difficulty in multi-LoRA MoE decode serving is not merely insufficient cache capacity, but a deeper fragmentation of locality once expert routing and adapter identity are modeled jointly. Under realistic GPU cache budgets, cold misses therefore become difficult to eliminate and increasingly concentrated in tail requests, making miss handling---rather than cache capacity alone---a first-order systems problem.

\subsection{Challenges}
\label{subsec:challenges}

The above observations reveal a fundamental shift in the role of caching. Under expert--LoRA joint-object modeling, locality degradation makes cold misses difficult to eliminate within practical GPU cache budgets. As a result, misses are no longer rare events that can be hidden behind a sufficiently large hot set; they become a dominant factor that must be explicitly managed on the latency-critical decode path.

This reframes the systems problem. The key question is no longer only how to reduce misses, but how to recover unavoidable misses without inflating per-token latency. Turning this insight into a practical runtime requires addressing three challenges.

\paragraph{Challenge 1: Reasoning at expert--LoRA granularity under expanded and fragmented working sets.}
\label{subsec:challenge-residency}

Under joint-object modeling, the effective key space expands substantially, while reuse becomes weaker and more fragmented across requests. This makes coarse-grained management ineffective: adapter-level residency retains many expert slices that are never touched, whereas expert-only reasoning overestimates reuse by ignoring adapter diversity. At the same time, naive fine-grained residency increases fragmentation pressure on the effective hot set and makes limited GPU capacity more vulnerable to transient cold objects.

The system must therefore reason at expert--LoRA granularity, which matches actual computation, without assuming that caching alone can absorb the resulting working-set expansion.

\paragraph{Challenge 2: Recovering cold misses without forcing promotion-first stalls on decode.}
\label{subsec:challenge-miss}

In this regime, many misses correspond to cold objects whose reuse is too limited to amortize the cost of immediate promotion. If the system continues to apply a conventional load-then-run policy, each such miss becomes a blocking recovery event: the current token must wait for weight migration before expert--LoRA computation can proceed. Under decode-time latency sensitivity, this directly amplifies tail latency.

The system therefore needs a different default recovery strategy for cold misses. Instead of forcing all misses through immediate GPU promotion, it must provide a way to complete the current expert--LoRA computation without making promotion the only recovery path, while still preserving the option to promote objects later when future reuse justifies it.

\paragraph{Challenge 3: Making cold-path recovery practical under decode constraints.}
\label{subsec:challenge-overlap}

Changing the miss-recovery abstraction is not sufficient by itself. If cold-path execution is too slow, the system merely replaces GPU promotion stall with CPU-side stall. If it is poorly integrated with the decode pipeline, heterogeneous execution introduces a new source of queueing and tail behavior rather than alleviating the original one.

The system must therefore make cold-path recovery efficient enough to operate at small, irregular decode batch sizes, and organize execution so that cold-miss handling can overlap with GPU hot-path and base-model computation whenever dependencies allow. The challenge is not only to provide an alternative recovery path, but to ensure that this path reduces exposed miss latency instead of becoming a new bottleneck.

\begin{figure*}[t]
  \centering
  \includegraphics[width=\textwidth]{figures/architecture.pdf}
  \caption{
  Overview of COLoRA's hot/cold dual-path serving design for expert--LoRA computation in decode.
  Routed expert--LoRA accesses first check GPU residency.
  Cache-hit objects are served on the GPU hot path.
  Cold misses are redirected to a CPU cold path, which completes the current LoRA computation via host-memory streaming, decoupled from the layer-wise synchronization of other active requests.
  Promotion is deferred to future reuse, and a temporal predictor prefetches top-2 experts to mask host-memory latency across sequential tokens.
  }
  \label{fig:colora_overview}
\end{figure*}

\section{Design}
\label{sec:design}

\subsection{Hot/Cold Execution-First Architecture}
\label{subsec:dual-path-handling}

COLoRA is motivated by a simple observation: under multi-tenant expert--LoRA serving, cache misses cannot be treated as rare exceptions. Once the unit of management becomes the expert--LoRA joint object, the effective working set expands and fragments, and cold misses remain common under realistic GPU memory budgets. In decode, where execution proceeds token by token and batching is limited, each such miss can directly appear as a stall on the current token's critical path.

The design objective is therefore not to eliminate every miss, but to prevent a cold miss from becoming a global synchronization event. COLoRA does so by changing the default recovery action. Instead of synchronously restoring the missing weights to the GPU and only then resuming execution, it adopts an execution-first policy: the system prioritizes completing the missing expert--LoRA computation using whatever resources are immediately available, while treating weight promotion as a separate optimization for future reuse.

More broadly, COLoRA is built around a uniform design philosophy: the mechanism that guarantees progress for the current token should be separated from the mechanisms that improve locality for future tokens. This separation allows miss handling to remain latency-oriented and correctness-critical, while promotion and prefetching remain opportunistic optimizations rather than mandatory recovery steps.

This leads to a hot/cold dual-path design for expert--LoRA computation in decode. When the required expert--LoRA object is resident, execution proceeds on the GPU hot path with no change to the normal fast path. When the object is absent, the request is diverted to a CPU cold path that completes the current LoRA computation using host-resident parameters. The key point is that the miss no longer forces the entire execution group to wait for immediate promotion.

The runtime realizes this behavior through a skip-and-reinsert discipline. If request $i$ encounters a miss at layer $L$, it is temporarily removed from the current GPU execution group, and its activation state is staged for host-side execution. The GPU then continues processing other ready requests at the same layer without waiting. Once the CPU cold path finishes the missing expert--LoRA computation, the result is returned to the device and request $i$ resumes from the next execution point. In effect, a cold miss delays only the affected request, rather than serializing all requests that happened to be grouped with it.

This design preserves three invariants that are central to correctness. First, each request still advances strictly in layer order. Second, different requests are allowed to be temporarily out of phase, which creates room for overlap rather than forcing lockstep execution. Third, a miss in one request does not block the progress of other ready requests. Together, these invariants allow COLoRA to convert unavoidable misses from global stalls into localized delay.

The design is particularly well matched to decode-time serving, where per-adapter batch sizes are small, routing decisions are heterogeneous across requests, and misses are not perfectly synchronized. In this regime, even modest overlap between hot-path GPU execution and cold-path CPU execution can substantially reduce the visible impact of cold misses. The next subsection therefore examines the cost structure of this execution-first recovery path and clarifies why its critical-path placement is more favorable than promotion-first recovery.

\subsection{Cost Structure of Cold-Path Execution}
\label{subsec:analytical-formulation}

Execution-first miss handling replaces synchronous weight promotion with a sequence that moves activations to the host, computes the missing LoRA contribution there, and returns the result to the device. The latency of a cold-path invocation can be decomposed as
\begin{equation}
T_{\text{cold}} = T_{\text{pack}} + T_{\text{D2H}} + T_{\text{cpu}} + T_{\text{H2D}} + T_{\text{merge}},
\end{equation}
where $T_{\text{pack}}$ denotes activation packing and layout preparation, $T_{\text{D2H}}$ and $T_{\text{H2D}}$ are device-to-host and host-to-device transfers, $T_{\text{cpu}}$ is host-side computation, and $T_{\text{merge}}$ is the cost of reintegrating the result into the device-side execution stream.

This decomposition matters because, in decode, the transferred activation payload is typically much smaller than the missing expert--LoRA weights themselves. As a result, execution-first avoids placing full weight movement on the current token's critical path. The dominant concern is therefore not whether the CPU path is universally faster in absolute time, but whether enough of its latency can be overlapped with ongoing GPU work on other requests.

For comparison, a promotion-first recovery path incurs
\begin{equation}
T_{\text{promote}} = T_{\text{admit}} + T_{\text{H2D-weights}} + T_{\text{gpu}},
\end{equation}
where $T_{\text{admit}}$ captures admission and scheduling overheads for the restored object, $T_{\text{H2D-weights}}$ is the host-to-device transfer of weights, and $T_{\text{gpu}}$ is the subsequent GPU execution. In expert--LoRA serving, the transfer and preparation of weights are often the dominant terms. More importantly, promotion-first keeps those costs on the synchronous recovery path of the current token.

The central distinction, then, is not merely one of total latency but of critical-path placement. Promotion-first makes recovery latency visible immediately because execution must wait for residency restoration. Execution-first instead turns recovery into a parallel side path whose visible cost is reduced whenever the hot path still has useful work to perform. This observation directly informs the runtime design: COLoRA must minimize fixed per-miss overheads, keep cold-path execution bounded and predictable, and preserve overlap whenever ready GPU work remains. We next describe the scheduler that realizes this decoupling in the runtime.

\subsection{Decoupled Miss-Recovery Scheduler}
\label{subsec:scheduler}

The execution-first design depends on a runtime scheduler that explicitly decouples cold-miss handling from the main GPU execution stream. Conceptually, each request alternates among three states: it is either ready to execute on the GPU hot path, being processed on the CPU cold path after a miss, or waiting to be reinserted once a cold-path result has returned. The scheduler maintains corresponding queues for GPU-ready work, CPU cold-path jobs, and completed results awaiting reintegration.

When a miss occurs at layer $L$, the scheduler removes the affected request from the GPU-ready set, materializes its activations into a staging buffer, and submits a cold-path job to the CPU side. Crucially, the GPU does not wait for this job to complete. It immediately continues issuing kernels for the remaining ready requests. When the cold-path job finishes, the result is transferred back to device memory and the request is reintroduced into GPU execution at the appropriate continuation point.

This organization gives the system \emph{decoupled miss recovery}: GPU progress is no longer tied to immediate weight promotion for every cold object. More precisely, the request that incurs the miss still waits for its own cold-path result before it can advance, but other ready requests do not need to synchronize on that recovery. COLoRA therefore does not eliminate cold-miss latency; instead, it isolates that latency to the faulting request and prevents a single miss from turning into a group-wide stall.

To keep the cold path from turning into an uncontrolled source of backlog, COLoRA uses an explicit bound on cold-path concurrency. Only a limited number of cold jobs may be in flight at once, and excess misses are queued rather than launched immediately. This keeps host-side resource usage predictable and avoids replacing one form of global instability with another. Within this bounded regime, the scheduler preserves per-request layer ordering, allows cross-request phase decoupling, and maintains forward progress on the GPU whenever ready work exists.

This scheduler is the runtime embodiment of COLoRA's core philosophy: demand execution is serviced immediately through the hot path or the cold path, while all other optimizations remain secondary to forward progress. Realizing that philosophy in practice, however, requires a cold path whose overheads are low and predictable enough to overlap with useful GPU work.

\subsection{CPU Cold Path Realization}
\label{subsec:cpu-cold-path}

The CPU cold path is designed not as a throughput replacement for the GPU, but as a predictable execution engine for unavoidable misses. Its job is to make execution-first recovery viable in decode by keeping miss handling bounded, overlap-friendly, and implementation-efficient.

The first step is activation packing. Before a cold-path transfer is issued, the relevant activations are reorganized on the GPU into contiguous staging buffers. This activation coalescing reduces fragmentation in DMA traffic and amortizes transfer overheads when multiple cold-path requests can be handled together. Because decode activations are relatively small, careful packing matters not only for bandwidth efficiency, but also for reducing fixed per-miss overheads.

Transfers between device and host use pinned memory and are issued asynchronously on separate CUDA streams. This allows device-to-host activation movement and host-to-device result return to overlap opportunistically with foreground GPU work, subject to available copy-engine capacity and scheduler state. The goal is not to assume perfect overlap, but to preserve it whenever the hardware and workload permit.

On the host side, missing expert--LoRA computations are executed by vectorized CPU kernels. COLoRA uses AVX-optimized implementations for the dense linear operations involved in LoRA execution, binds worker threads to dedicated cores to reduce scheduling jitter, and NUMA-aligns those workers with the GPU's PCIe root complex when applicable. These choices are intended to keep $T_{\text{cpu}}$ stable and predictable under repeated decode-time invocations. The system does not rely on the claim that CPU execution is generally faster than GPU execution. Instead, it relies on the more realistic and more important claim that the cold path can be made sufficiently efficient and sufficiently stable to overlap with useful GPU work in mixed hit/miss batches.

Once the host computation completes, the result is copied back and merged into the device-side execution state. This reintegration step is deliberately lightweight and avoids introducing a global barrier. In combination, activation coalescing, asynchronous staging, and vectorized host execution turn the CPU into a practical cold-path engine rather than a fallback of last resort.

With this execution substrate in place, COLoRA can cleanly separate the mechanism that serves a miss now from the mechanisms that improve how likely similar accesses are to be served more efficiently later. We therefore turn to the locality-driven optimizations layered on top of the execution-first core.

\subsection{Locality-Driven Background Optimizations}
\label{subsec:locality-background}

Beyond execution-first miss recovery, COLoRA incorporates two locality-driven background optimizations that improve future access efficiency without reintroducing promotion-first behavior on the current token's critical path.

The first is deferred promotion. While COLoRA removes blocking promotion from the foreground miss-recovery path, promotion remains important for improving future GPU residency. The key design change is therefore not to eliminate promotion, but to redefine its role. In COLoRA, promotion no longer serves as the default recovery mechanism for the current miss. Instead, it is treated as a background optimization whose purpose is to increase the likelihood that subsequent accesses are served on the GPU hot path.

When a cold-path execution is triggered, COLoRA may asynchronously enqueue the corresponding expert--LoRA object for promotion into GPU memory. This promotion is issued off the critical path and does not delay completion of the current token. In effect, cold-path execution handles the present access, while deferred promotion improves cache residency for future accesses if the object is likely to be reused.

To prevent promotion from degenerating into indiscriminate background traffic, COLoRA employs a reuse-distance-aware admission policy. Let $\delta$ denote a reuse threshold. An object is admitted for asynchronous promotion only when it is expected to be reused within the next $\delta$ decode steps. Objects whose predicted reuse distance exceeds $\delta$ remain host-resident and continue to be served on demand through the cold path. This policy avoids wasting PCIe bandwidth and GPU cache capacity on weakly reusable objects, and preserves the benefit of removing blocking promotion from the foreground execution path.

In the current design, $\delta$ is implemented as a static hyperparameter. A more adaptive policy that tunes $\delta$ online based on runtime signals, such as observed reuse behavior, PCIe queue depth, or device-memory pressure, is left to future work. Importantly, admission errors affect only future cache efficiency: they do not delay the current request and do not alter COLoRA's execution-first miss-handling semantics.

The second optimization exploits short-horizon temporal locality in decode to reduce effective cold-path service time. This design is motivated by an empirical observation from our traces: for a given layer, adjacent decode steps share the same top-1 routed expert in more than 95\% of cases. Although long-horizon reuse of expert--LoRA objects can be weak under multi-tenant serving, this strong step-to-step routing continuity makes near-future accesses substantially more predictable than global cache behavior might suggest.
% TODO: motivation 中加入相应观察

COLoRA leverages this regularity using a lightweight temporal predictor that identifies expert candidates likely to be accessed in the next few decode steps and opportunistically prefetches the corresponding host-resident weights. Importantly, prefetching does not participate in demand execution: a request never waits for a prefetched object, and cold-path execution remains the default fallback when the required object is not yet available. The purpose of this mechanism is therefore not to guarantee stable residency in any particular CPU cache level, nor to assume that large expert weights remain fully resident in LLC. Rather, it improves effective access locality and reduces the service time of subsequent cold-path invocations when the prediction is correct.
% TODO: 是否需要说明 prefetch 的对象很小 ~几百KB level，所以足以放在 LLC 上而不影响别的计算？

Because temporal prefetching is auxiliary to the main execution-first design, incorrect predictions consume only bounded background bandwidth and do not affect correctness or foreground progress. Taken together, deferred promotion and temporal prefetching preserve COLoRA's uniform design philosophy: misses are served immediately through execution-first recovery, while locality-driven background mechanisms optimize how similar accesses are handled later.

% \section{Implementation}
% \label{sec:implementation}
% TODO:WIP

% \section{Evaluation}
% \label{sec:evaluation}
% TODO:WIP

% We evaluate COLoRA along five dimensions that correspond directly to the claims made in this paper. In particular, we ask the following questions:

% \begin{itemize}
% \item \textbf{RQ1: End-to-end latency.} Does COLoRA reduce end-to-end decode latency under multi-LoRA workloads?
% \item \textbf{RQ2: Tail latency.} How much does COLoRA improve P95/P99 latency under long-tail expert--lora access patterns?
% \item \textbf{RQ3: Miss handling.} How effective is COLoRA's non-blocking miss path, which combines a GPU hot cache with CPU fallback, relative to conventional load-then-run execution?
% \item \textbf{RQ4: Component contribution.} What performance gain is attributable to each major component, including CPU fallback and speculative dispatch?
% \item \textbf{RQ5: System behavior.} How does COLoRA affect cache hit rate, miss cost, overlap efficiency, and overall resource utilization?
% \end{itemize}

% \subsection{Hardware and System Configuration}
% \label{Configuration}
% All experiments are conducted on a single server with the following configuration:

% \begin{itemize}
% \item \textbf{GPU:} A100 80GB / RTX 6000
% \item \textbf{CPU:} Intel Xeon Gold 5520+ (1 socket, 28 physical cores, 56 hardware threads, AVX-512 support)
% \item \textbf{Memory:} 376GB RAM
% \item \textbf{Interconnect:} PCIe Gen4 / NVLink
% \end{itemize}

% Unless otherwise noted, the CPU fallback path uses AVX-512-optimized kernels.


% \subsection{Models and Workloads}
% \label{subsec:workloads}
% We instantiate COLoRA on the public \textbf{Qwen3-VL-30B-A3B-Instruct} checkpoint. Although Qwen3-VL is a multimodal model, our evaluation targets its MoE text decoder because COLoRA operates on decode-time expert--lora management. The evaluated decoder has 48 layers, hidden size 2048, 128 routed experts per layer, and top-8 routing for each token at every sparse layer. For adapter experiments, we use rank-16 LoRA modules applied to the attention and MLP projections, including the MoE gate, up, and down projections.

% All workloads target the decode worker in a PD-separated serving pipeline. We collect real decode-phase routing traces by replaying a fixed corpus of 512 ShareGPT V3 requests through the model and recording the router-selected experts at each token. We then pair this routing stream with semi-realistic adapter-invocation traces derived from the Azure Functions workload trace, which preserves realistic tenant-arrival skew while allowing controlled variation in adapter-side multiplexing.

% Unless otherwise specified, we evaluate both independent and correlated tenant-to-adapter mappings and sweep the active adapter cardinality over $\{8, 32, 128\}$. We further vary the decode batch size (typically 1--4) and the request concurrency at the decode worker to test how COLoRA behaves as joint expert--lora fragmentation and system contention increase together.

% \subsection{Baselines}
% \label{subsec:baselines}

% The main comparison should include an optimized GPU-only baseline that keeps GPU as the only execution target and uses the best available caching and prefetching policy. A second baseline should adopt joint-object cache management but still resolve misses by blocking promotion, which isolates the value of heterogeneous miss handling from the value of finer-grained residency alone. Finally, the paper should include COLoRA ablations, including at least variants without CPU fallback, without asynchronous overlap, and without the optimized MoE CPU kernel path.

% % We compare COLoRA with the following baselines:

% % GPU-Only:
% % All LoRA–expert weights reside on GPU. No offloading.

% % Naive Offload (Load-Then-Run):
% % On a cache miss, weights are synchronously transferred from CPU to GPU before execution.

% % [Optional] Static Partitioning:
% % A static subset of LoRA–expert pairs is pinned in GPU memory.

% \subsection{End-to-End Latency}
% \label{subsec:e2e-lat}

% % The primary latency metric should be TPOT at mean, P90, and P99 because the paper is fundamentally about decode stability. Throughput is also necessary to show that tail-latency gains are not purchased with unacceptable aggregate slowdown. TTFT should be reported as well to verify that decode-side changes do not create obvious collateral damage in the broader serving path.

% We first evaluate whether COLoRA improves end-to-end decoding latency.
% % TODO:
% Figure X shows the average and tail latency (P95/P99) across different workloads.

% COLoRA reduces average latency by up to X\% and P99 latency by up to Y\% compared to Naive Offload.

% This improvement comes from avoiding blocking weight transfers on cache misses.

% Instead of stalling on load-then-run, COLoRA executes miss requests via CPU fallback,
% thereby converting latency spikes into more stable execution paths.

% \subsection{Tail Latency Reduction}
% \label{subsec:tail-lat}

% % The evaluation should also expose the runtime mechanics that explain the end-to-end behavior. At minimum, the paper should report cache hit rate and miss breakdown, CPU queue depth and queue wait, CPU compute time, GPU compute time, overlap ratio, promotion-drop breakdown, and host memory footprint. These measurements let the paper demonstrate whether COLoRA improves P99 because it changes miss handling in the intended way, rather than because of an unrelated system effect.

% We analyze the impact of COLoRA on tail latency under skewed access patterns.

% Under high LoRA concurrency, Naive Offload exhibits significant tail amplification,
% with P99 latency increasing sharply.

% COLoRA reduces P99 latency by up to X\times.

% This is because COLoRA decouples cache miss from request blocking.

% Long-tail accesses no longer incur full transfer latency,
% but are instead handled via CPU fallback with bounded overhead.

% % �� 关键词要反复打：

% % “decoupling miss from blocking”

% % “bounded miss cost”

% \subsection{Effectiveness of Non-Blocking Miss Handling}
% \label{subsec:miss-handling}
% We evaluate how COLoRA’s miss handling policy affects system behavior.

% Metrics need to report:

% miss rate

% miss latency

% fallback ratio

% COLoRA significantly reduces effective miss penalty.

% While the miss rate remains similar, the average miss cost decreases by X\%,
% as many misses are served via CPU fallback instead of blocking transfers.

% This demonstrates that optimizing miss handling policy is as important as improving cache hit rate.

% Even when misses are unavoidable, COLoRA ensures they are handled in a non-blocking manner.

% \subsection{Breakdown Analysis}
% \label{subsec:breakdown}
% We break down end-to-end latency into major components.

% TODO: figures

% stacked bar:

% GPU compute

% CPU fallback

% transfer

% idle/wait

% COLoRA reduces idle time caused by blocking transfers.

% Although CPU fallback introduces additional compute,
% it overlaps with GPU execution and avoids long stalls.

% \subsection{Ablation Study}
% \label{subsec:ablation}
% We evaluate the contribution of each component.

% - Full COLoRA
% - w/o fallback (always load-then-run)
% - w/o speculative dispatch

% Removing fallback significantly increases tail latency,
% confirming its role in mitigating miss-induced stalls.

% Speculative dispatch further improves performance by overlapping CPU and GPU execution.

% % “救命 checklist”

% % 要保证：

% % ✅ 有 end-to-end latency 图
% % ✅ 有 P99 改善
% % ✅ 有 baseline 对比
% % ✅ 有 1–2 个 insight（不是纯图）

% % ============================ GPT Notes ============================
% % Compact outline:
% % - The paper targets PD-separated decode workers that serve an MoE base
% %   model together with many concurrently active LoRA adapters.
% % - The core observation is that the effective managed object is no longer
% %   an expert or an adapter alone, but a joint expert-adapter object whose
% %   locality is much weaker and more fragmented.
% % - A trace-driven case study shows a consistent chain: flatter popularity,
% %   worse temporal locality, higher miss rates under the same budget, and a
% %   tail penalty that turns on early once the system leaves the single-LoRA
% %   regime.
% % - The resulting systems problem is not just cache sizing. It is how to
% %   handle unavoidable misses without exposing weight migration on the decode
% %   critical path.
% % - COLoRA addresses this problem with an asymmetric memory hierarchy,
% %   CPU-first non-blocking miss handling, selective asynchronous promotion,
% %   and overlapped heterogeneous execution.
% % - The evaluation should focus on P99 TPOT, throughput, miss-handling
% %   breakdown, and the cost profile of the CPU fallback path.
% %
% % Reverse outline:
% % - Introduction thesis: joint-object fragmentation makes decode misses
% %   structural, so the system must optimize miss handling, not just miss
% %   avoidance.
% % - Introduction paragraph 1: PD-separated MoE multi-LoRA decode serving is
% %   tail-sensitive.
% % - Introduction paragraph 2: the managed object becomes a joint
% %   expert-adapter object.
% % - Introduction paragraph 3: the case study shows flatter popularity, worse
% %   locality, and early tail turn-on.
% % - Introduction paragraph 4: GPU-only load-then-run is a poor default once
% %   cold misses become structural.
% % - Introduction paragraph 5: COLoRA handles hot and cold objects
% %   differently through a dual-path runtime.
% % - Background thesis: the joint-object view changes both the memory problem
% %   and the execution problem.
% % - Challenges thesis: the runtime must control residency churn, CPU fallback
% %   cost, and heterogeneous synchronization together.
% % - Design thesis: COLoRA keeps hot objects on GPU, executes cold misses from
% %   CPU, and only promotes when reuse signals justify it.
% %
% % Self-review checklist:
% % - Clarity: Each section has an explicit thesis sentence and stable
% %   terminology around joint expert-adapter object, GPU hot cache, and CPU
% %   fallback.
% % - Flow: The story moves from setting -> failure mode -> evidence -> design
% %   question -> design overview.
% % - Terminology consistency: The paper reasons about logical joint objects
% %   and notes that the implementation realizes them as projection-specific
% %   entries.
% % - Unsupported claims: The draft avoids claiming measured end-to-end gains
% %   that are not yet written down.
% % - Missing evidence: The final paper still needs full end-to-end COLoRA
% %   results, especially TPOT/P99 comparisons against strong GPU-only
% %   baselines and ablations for queue control and CPU kernels.
% %
% % Claim-evidence map:
% % - Claim: In MoE multi-LoRA decode serving, the effective managed object is
% %   a joint expert-adapter object rather than an expert or adapter alone.
% %   Evidence: The request needs LoRA residuals only for the experts selected
% %   by the router; the implementation also keys GPU residency by projection,
% %   adapter, layer, and expert. Status: supported.
% % - Claim: Joint-object modeling significantly fragments popularity and
% %   weakens temporal locality. Evidence: Top-1000 access coverage drops from
% %   89.1% to 40.7% and 32.6%; finite P99 reuse distance grows from 1,393 to
% %   2,041 and 4,521; cold first touches rise from 1.0 to 9.7 and 13.6 per
% %   10k events. Status: supported.
% % - Claim: Under the same cache budget, joint-object fragmentation amplifies
% %   misses enough to become a tail-latency problem. Evidence: At 2048
% %   objects, miss rate rises from 0.00159 in B0 to 0.01095 and 0.01476 in
% %   B1/B2; the 8192-object sweep shows a P99 latency-proxy penalty of
% %   411.7 ms by four LoRAs and a broad high plateau afterward. Status:
% %   supported.
% % - Claim: Tail requests are dominated by cold joint-object touches rather
% %   than by a uniform slowdown across all requests. Evidence: At the
% %   65536-object cold-miss floor, average requests touch 83.6 and 116.8 cold
% %   joint objects, while P95+ tail requests touch 912.8 and 1154.0. Status:
% %   supported.
% % - Claim: COLoRA's key design shift is to treat a miss as an execution-path
% %   decision instead of a mandatory blocking promotion event. Evidence: The
% %   runtime maintains a GPU hot cache plus a CPU full replica; the configured
% %   default miss policy is cpu_first; misses can execute immediately on CPU
% %   while promotion is queued asynchronously for future reuse. Status:
% %   supported.
% % - Claim: CPU fallback is plausible only if the runtime keeps the fixed cost
% %   of sparse decode misses low. Evidence: The implementation includes
% %   MoE-specific CPU kernels, adapter-grouped miss execution, asynchronous CPU
% %   queueing, and explicit queue/overlap counters. Status: supported for
% %   mechanism, needs end-to-end performance evidence for effect.
% % - Claim: Overlap between GPU hits and CPU misses is an important part of
% %   the design rather than an implementation detail. Evidence: The hybrid
% %   path tracks CPU compute time, GPU compute time, queue wait, overlap
% %   ratio, and fallback degradations; mixed hit-miss tests exercise this
% %   path. Status: supported for mechanism, needs end-to-end performance
% %   evidence for effect.



% \section{Related Work}
% \section{Conclusion}
\clearpage
\bibliographystyle{plain}
\bibliography{COLoRA-reference}

\end{document}
\endinput
%%
%% End of file `sample-sigconf.tex'.
